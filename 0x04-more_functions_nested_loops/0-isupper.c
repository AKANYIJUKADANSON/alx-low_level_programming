#include "main.h"

/**
 * _islower - checks for an uppercase character
 * and the character to check is c
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}

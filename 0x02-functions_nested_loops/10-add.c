#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a and @b: are the two integers to add
 * Return: return result
 */
int add(int a, int b)
{
    int x;
    x = a + b;
    _putchar(x);
    return (0);
}
